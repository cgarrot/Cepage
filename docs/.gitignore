# Local / internal documentation under docs/ should stay out of git.
# Tracked exceptions:
# - getting-started-compiler.md — public compiler guide linked from the root README
# - workflow-prompt-library/** — shipped workflow catalog (loaded at runtime by
#   packages/api/src/modules/workflow-skills/workflow-skills.service.ts)
# - this file — so contributors share the same ignore rules

**
!/.gitignore
!/getting-started-compiler.md
!/workflow-prompt-library/
!/workflow-prompt-library/**

# Internal PRD / beta collateral — local only, not shipped
product-plan/
workflow-prompt-library/beta-test/
